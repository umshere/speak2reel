# Environment variables for podcast-to-reels

# Example API Key (replace with your actual key)
API_KEY="YOUR_API_KEY"

# OpenAI API Key for Whisper transcription, GPT models, and image generation (DALL-E 3/GPT-4o)
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Path to FastText language identification model (lid.176.bin)
# Optional: The script has a default, but you can override it by setting this in your .env file
# or by using the --fasttext_model_path command-line argument.
# FASTTEXT_MODEL_PATH="path/to/your/lid.176.bin"

# Example Database URL (replace with your actual database URL)
DATABASE_URL="YOUR_DATABASE_URL"

# Example Secret Key (replace with your actual secret key)
SECRET_KEY="YOUR_SECRET_KEY"
